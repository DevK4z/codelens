# Cấu hình API Backend cho CodeLens
# Trong môi trường local development, Vite sẽ tự động trỏ tới http://localhost:3001/api (không cần đặt biến này).
# Để trỏ tới backend thực sự trên production (ví dụ khi deploy Frontend lên GitHub Pages), hãy đặt biến này:
# VITE_API_BASE_URL=https://your-real-backend.com/api
