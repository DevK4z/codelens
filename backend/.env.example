# Set these environment variables in the hosting service or shell.
# This file is a template; server.ts does not automatically load .env without dotenv.
NODE_ENV=production
HOST=0.0.0.0
PORT=3001
CORS_ORIGINS=https://devk4z.github.io
RUNNER=docker
